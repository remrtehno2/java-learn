public class Chalenge1 {
    public static void main(String[] args) {
        /* String outText = "P";
        System.out.print(outText);

        System.out.print("P");

        // запрашивае у пользователя и записывает в переменную
        int tmp = 0;
        Scanner scanner = new Scanner(System.in);
        tmp = scanner.nextInt();

        if (tmp > 22) {
            System.out.print("teplo");
        } else {
            System.out.print("holodno");
        }


 */

      /*  int x = 0;
        Scanner scanner = new Scanner(System.in);
        x = scanner.nextInt();

        int y = (7 * x) + 5;

        System.out.print(y);
*/


        /* int a = 0;
        int b = 0;
        Scanner scanner = new Scanner(System.in);
        a = scanner.nextInt();
        b = scanner.nextInt();

        double p = 0.0d;
        p = a * 0.45;

        if (p > b) {
            System.out.print("km bolshe");
        } else {
            System.out.print("fut bolshe");
        }

*/

        /*
        int pogodaTmp = 18;
        boolean isRaining = true;

        if(pogodaTmp > 22) {
            if(isRaining == false) {

            }
        }


        // AND
        if(pogodaTmp > 22 && !isRaining) {

        }

        // OR
        if(pogodaTmp > 22 || isRaining == false) {

        }
         */

    }
}
